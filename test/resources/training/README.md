# Test fixtures for training TOML parse / projection / roundtrip.
#
# These are *not* the live operator configs under configs/training/ (molab sync).
# Dial priors, tau, lambdas, and step budgets in configs/training/ freely;
# update fixtures only when the TOML *schema* or projection contract changes.
